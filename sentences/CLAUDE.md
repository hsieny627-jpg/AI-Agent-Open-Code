# 英文句型網站 — 規格（接手的 Claude 先讀完再動手）

使用者是國小英語教師。**這份規格是使用者指定的結果，不要自行「優化」。**
根目錄 `AGENTS.md` 第四節（品質與省額度的固定規則）同樣適用。

## 這是什麼

三年級英文句型教學網站，四個部分：**首頁 → 暖身題 → Unit 1（16 張）／Unit 2（21 張）句型卡
（每一頁都內建「📝 複習」）→ 複習遊戲**。
全部單檔、零外部相依（字體吃 `../words/fonts/andika-*.woff2`，離線也是 Andika）。
入口：`sentences/index.html`，根目錄首頁第 8 張卡連過來。

## 檔案分工（動手前先看這裡）

| 檔案 | 說明 |
|---|---|
| `_data.js` | **Unit 1／Unit 2 句型卡的唯一真相來源**：token（英文／中文／圖示）、替換字、底色、情境（`SC1`／`SC2`）、**複習題（`RV1`／`RV2`）** |
| `_quiz_data.js` | **暖身 22 題的唯一真相來源**，網頁版與 Kahoot 都從這裡拿 |
| `_game_data.js` | **10 個遊戲題庫的唯一真相來源**，每個遊戲 ≧ 20 題，**外加每個遊戲自己的 20 種驚喜回饋（`SURP`）** |
| `_shared.js` | 五頁共用的 `<head>`、配色、發音（TTS）、**語速六段**、小工具。**改版面先改這裡**（音效已依使用者指定暫時刪除，只留空殼函式） |
| `_build.js` | **一次重建整站**：`node sentences/_build.js` |
| `_build_home.js` / `_build_cards.js` / `_build_quiz.js` / `_build_games.js` | 四個產生器 |
| `_build_kahoot.js` | 產出根目錄 `kahoot_sentences_22.xlsx` ＋ 上架說明 |
| `_verify.js` | 量測腳本。`node sentences/_verify.js` 量全部；`node sentences/_verify.js unit1.html` 只量一頁 |
| `index.html` `warmup.html` `unit1.html` `unit2.html` `games.html` | **產物，不要手改**，會被下一次 build 蓋掉 |

## 字卡的七條規則（使用者指定，不要改回去）

1. **卡片底色純黑 `#000`。**
2. **每一個英文字正下方是那個字的中文**，包括 `Who's` 的 `'s` 底下標「是」、
   `He's` 的 `'s` 底下也標「是」。做法：`'s` 自己是一個獨立 token（`tight:1` 讓它黏住前一個字）。

   **`'s` 的發音一定要是 /z/**（使用者 2026-09-20 指定）：不可以唸成「ess」或「撇號 s」。
   做法寫在 `_build_cards.js` 的 `tkHTML()`：`'s` 的 `data-say` 自動變成「前一個字 ＋ 's」，
   所以點下去唸的是 `Who's` /huːz/、`He's` /hiːz/ ——學生聽到的就是真人聲音裡的那個 /z/。
   **為什麼不直接唸一個 z**：瀏覽器內建語音唸單獨一個子音會變成字母名「zee」，那是錯的；
   放在字裡才是正確的 /z/，而且離線、每一種瀏覽器都對。
3. **中文正下方再一層是秒懂圖示**（emoji），可以用 `圖示 Y/N` 按鈕開關。
   **標點符號（`.` `,` `?`）一律不給圖示**，`Who` 的圖示是純 `❓`（不是人形）。
   使用者 2026-09-20 指定：圖示太多版面很亂，而且 `Who` 用人形會跟 `he 👦` 撞在一起，
   學生分不出來。**不要再把圖示加回標點符號上。**
4. **所有撇號 `'` 一律紅色**（`.ap`，`ap()` 這個函式負責上色）。
5. **四個 Y/N 開關**（使用者 2026-09-20 指定，取代原本的五選一模式列）：

   | 按鈕 | Y | N |
   |---|---|---|
   | `英文 Y/N` | 看得到英文 | 藏起來 |
   | `中文 Y/N` | 看得到每個字下方的中文 | 藏起來 |
   | `圖示 Y/N` | 看得到秒懂圖示 | 藏起來 |
   | `整句中文 Y/N` | 看得到整句翻譯 | 藏起來 |

   **四個各自獨立**，開幾個關幾個都可以（預設四個全 Y）；做法是在 `#card` 上加
   `no-en`／`no-zh`／`no-ic`／`no-full`。**關掉上面幾層，剩下的那一層會自動放大**
   （只剩中文 → 中文變大；只剩圖示 → 圖示變大），這樣任何組合都還有一個主角。
   **不要改回五選一的模式列。**

6. **`Who's he?` 的整句翻譯一定是「他是誰？」**，不可以寫成「誰是他？」。
   `order` 語序卡把這件事**演出來**：上排中文、下排英文，同一個顏色上下對照
   （2026-09-21 起兩排放在**同一個 `.ordgrid`**、欄寬 `max-content`、`.chip` 不折行——
   這樣 `Who` 的 `ho` 才不會被折到下一行，而且上下還是對得齊），
   一進到這張卡，**下排英文的字會從上排中文自己的位置飛過去**（FLIP 動畫，`playOrder()`），
   學生自己就看到紅色的「誰」跑到最前面變成 `Who`。按 `🔁 再演一次` 可以重播。
   **這張卡不要寫文字說明**——會動的東西講完了，再寫一次就是廢話（見下面第 8 條）。
7. **點什麼唸什麼**：點單字唸單字、點整句中文唸中文（zh-TW）、點卡片唸整句。
   語速用底部的 **`🗣 語速` 六段**（見下面「語速六段」），發音一律 **en-US**，
   優先挑 Samantha／Ava／Alex。

### 顏色三條（2026-09-21 使用者指定，不要改回去）

| 要看到什麼 | 做法 | 為什麼 |
|---|---|---|
| `Who` 的 **w 淺灰色** | `enHTML()` 看到字首是 `Who` 就把 W 包成 `.sil` | **w 不發音**，唸 /huː/。顏色講完，不用再寫一行字 |
| `What` 的 **h 淺灰色**（2026-09-21 使用者指定） | `enHTML()` 看到字首是 `Wh` 但不是 `Who`，就把第 2 個字母包成 `.sil` | **h 不發音**，唸 /wɑt/。Unit 1 第一張秒懂重點卡看得到 |
| `is` 的 **i**、`not` 的 **o** 是**紅色** | token 寫 `ri:'i'`／`ri:'o'`，`enHTML()` 上 `.ri` | 紅色的字母 ＝ 等一下會被紅色的 `’` 藏起來 |
| Unit 1 的 `she`／`She` **淺粉底**（`lp`） | token 寫 `hl:'lp'` | 原本的桃紅跟 `Who` 的紅太接近，學生分不出來 |

`he`／`He` 一律**藍底**（問 he ➜ 答 He）。說明文字裡想讓某個字母變紅，寫 `[i]`，`mk()` 會上色。

### 三種會動的新卡片（2026-09-21 使用者指定新增）

| type | 在哪裡 | 演什麼 |
|---|---|---|
| `morph` **縮寫變身** | Unit 1 ✕ 3（Who is／He is／She is）、Unit 2 ✕ 4（He is／She is／is not ✕ 2） | **五拍**（2026-09-21 使用者指定）：① 完整唸出兩個字 `Who is` ➜ ② 紅色的 **i**（`not` 是 **o**）飛走、紅色的 **’** 站到同一個位置 ➜ ③ 兩個字滑過去黏成一個字 ➜ ④ 完整唸出一個字 `Who’s` ➜ ⑤ 底下亮出 **`Who is ＝ Who’s`**，兩句連著再唸一次。點等號兩邊各唸各的，按 `🔁 再演一次` 重播 |
| `echo` **他問他答** | Unit 1 第 14 張（取代原本的文字說明） | 問句裡藍底的 `he` **自己飛到**答句的 `He` 上面，`she` 同理（淺粉）。答句用 `______`，不用「……」 |
| `swapdemo` **直述句 ⇄ 問句** | Unit 2 第 19 張（取代原本的文字說明） | 自己演三次：藍色和黃色滑過去交換，**句號變問號**會放大跳一下（`.d2q`） |

`eq` **縮寫卡**（2026-09-21 使用者指定改版）：進到卡片會**自己演一次**——
上面那一句先亮起來並**完整唸出來**（`Who is he?`），換下面那一句亮起來**完整唸出來**（`Who’s he?`），
最後**兩句一起亮、中間的 ＝ 放大跳一下**。按 `🔁 再聽一次` 重播，點哪一句唸哪一句。
做法在 `playEq()`，用 `after(句子, 最多等幾毫秒, 下一步)`——**唸完才換下一拍**，
所以語速調到 0.5 也不會畫面跑在聲音前面。

`pair` **一問一答卡**（2026-09-21 使用者指定改版）：問句和答句都改成**逐字**，
**每一個英文字的正下方就是那一個字的中文**（跟句型卡一樣的 `.tk` 三層），Unit 1／Unit 2 都一樣。
資料寫在 `_data.js` 的 `qtk`／`atk`（token 陣列）＋ `qzh`／`azh`（整句中文）。
**問句和答句的 he／He 都是藍底**（she／She 淺粉／粉），問句那個有底色的字會飛到答句；
**卡片最下面有替換字**，換完兩句的發音、整句中文、圖示都跟著換（`tkLists()`／`setSlot()`）。

### 📝 複習（2026-09-21 使用者指定新增）

`unit1.html`／`unit2.html` 底部都有 `📝 複習`：**每 4 張卡一組**（Unit 1 四組、Unit 2 五組（最後一組 5 張）），
一組至少 3 題，**每題限時 20 秒，愈快答對分數愈高**（100 分 ＋ 速度分最多 900）。
題目與選項**每一次都重洗**，答錯給一句話的鷹架，結算列出要記住的每一條，
最佳紀錄存在這台 iPad（`sent_rv_<unit>_<組>`）。題目改 `_data.js` 的 `RV1`／`RV2`。

### 🗣 語速六段（2026-09-21 使用者指定，取代 `🐢 放慢`）

五頁底部都有 `🗣 語速`：**0.5／0.6／0.7／0.8／0.9／1.0，1.0 ＝ 正常語速**，預設 0.9。
按下去存在這台 iPad（`sent_rate`），下一次打開還是同一段。
做法在 `_shared.js`：`RATE` 直接餵給 `SpeechSynthesisUtterance.rate`（中英文都一樣），
`setRate()` 順便更新 `SLOW`（0.8 以下算慢，自動播的字間隔會拉長）。
按鈕列 HTML 是 `S.RATEBAR`、綁定是 `S.RATEJS`，**五頁共用，不要各寫各的**。

### 🔊 音效：暫時刪除（2026-09-21 使用者指定）

**音效和 `🔊 音效 Y/N` 按鈕都拿掉了。** `_shared.js` 的 `SFX` 只留空殼
（`beep`／`sOk`／`sNo`／`sTick`／`sPop`／`sWow` 什麼都不做），
五頁的呼叫點原封不動——**要加回來只改 `_shared.js` 那一段**，不用再去翻四個產生器。
**只刪音效，發音照舊**：上課還是要聽得到英文。

### 🔊 發音引擎（2026-09-21 整個換掉，`_shared.js`）

使用者回報「自動播有些字沒聲音、整句發音延遲、點句子和念一次常常沒反應」。三個原因、三個修法：

| 原因 | 修法 |
|---|---|
| 每次 `say()` 都先 `cancel()`，前一個字被砍掉 | **排隊**：`say(字,null,{keep:1})` 排在後面，一個講完才講下一個；自動播改成「唸完才出下一個字」（`say(...,{done:fn})`） |
| Chrome／Edge 老 bug：`cancel()` 之後馬上 `speak()` 會被吃掉 | `cancel()` 之後**等 90ms** 再講 |
| 講到一半不發 `onend`、或自己停住 | **看門狗**每 0.4 秒檢查一次，卡住就救回來（順便 `resume()`） |

**`＝`（還有 `➜`、`……`、`______`）一律不發音**：`sayText()` 在唸出來以前先拿掉（使用者指定）。
**`is` 的圖示用「＝」這個字，不要用 🟰**——使用者的電腦沒有那個 emoji，只看得到一個白框。

### 🔤 點中文唸：中文／英文（2026-09-20 使用者指定新增）

使用者原話「點擊英文單字、英文句型**和中文翻譯**都會產生標準美式英語發音」有兩種解讀，
使用者選了**兩種都要，用按鈕切**：

- `🔤 點中文唸 中文`（預設）：點中文唸中文（zh-TW）。
- `🔤 點中文唸 英文`：點中文唸**那一句對應的英文**（看中文、聽英文，是真正在練）。

做法：每一個中文元素都帶 `data-zh` ＋ `data-en` 兩個屬性，`sayPair()` 依開關挑一個。
**新增任何一個中文元素，一定要一起加 `data-en`**，否則切到「唸英文」會沒聲音——
`_verify.js` 會逐張量這件事。

### 🎞 情境：真實情境圖（2026-09-20 使用者指定新增）

**老師按按鈕才出現，平常完全不佔版面。** 28 張卡**每一張都有**，寫在 `_data.js` 最下面的
`SC1`／`SC2`（一張卡一個，照順序對應，改情境只改那一個地方）。

三行講完，多一行都不行：

| 欄位 | 內容 | 例 |
|---|---|---|
| `at` | 場景：在哪裡、什麼時候 | `🏫 校門口・放學時間` |
| `pic` | 秒懂畫面：誰對誰做什麼（大 emoji） | `👦💬 ➜ 👉 ➜ 👨` |
| `use` | 什麼時候會這樣說（一句話，要有看得見的主詞） | `看到同學旁邊站著一個不認識的男生，你想問他是誰。` |

**為什麼是 emoji 不是照片（使用者 2026-09-20 同意）**：製作環境連不到圖庫，
而且網站必須離線也能上課（教室網路常斷）。emoji 投影清楚、改起來快、不會有版權問題。
**要換成真照片**，就在 `.scene` 的 `spic` 位置放 `<img>`，圖檔放 `sentences/scenes/`。

**英文一律正確**：使用者舉例寫的「He's my sister.」是錯的（sister 是女生，主詞只能用
**She**），已訂正。使用者 2026-09-20 同意用正確版本，**不要改回去**。

**情境畫面會動**（2026-09-20 使用者指定）：`pic` 用 `➜` 切段，產生器會讓它
**一段一段跳出來**（`.spic .sg`／`.sar`，`sgIn` 動畫），不是一次全部塞出來。
所以 `pic` **最多三段**，`at` 最多 8 個字，`use` 最多 12 個字——
使用者 2026-09-20 指出原本「字太多、圖示凌亂、看不懂」，已全部改短，**不要再寫長**。

**版面**：情境放在卡片**最上面**（先有情境，才有句子）。加了情境卡片會變高，
`fit()` 會自動等比縮小並把倍率寫進 `#card` 的 `data-k`；
`_verify.js` 會量「**縮放倍率不可以低於 0.62**」——再小最後一排就看不清楚了。
目前 28 張卡兩種尺寸都是 `k = 1.000`（完全不用縮）。

### 版面三大重點（使用者 2026-09-20 指定，優先於任何「看起來比較豐富」的想法）

**版面簡潔清爽、廢話少、秒懂動畫多。** 具體五條：

1. **字體大**。英文 `clamp(30px,8.6vh,82px)`、逐字中文 `clamp(19px,3.6vh,36px)`、
   整句中文 `clamp(23px,4.3vh,44px)`、對話卡中文 `clamp(20px,3.6vh,36px)`。
   量測結果：28 張卡在兩種尺寸、情境開與關，**縮放倍率全部都是 1.000**（完全不用縮）。
2. **廢話一律刪掉**。使用者點名的廢話：「兩句一模一樣。」「誰在中文的最後一個字，
   Who 在英文的第一個字。」——**畫面已經看得到的事，不可以再用文字講一次**。
   `eq` 縮寫卡現在沒有整句中文、也沒有「＝」的文字說明，因為 `＝` 本身就講完了。
   新增任何一行字以前先問：**這一行有沒有在講畫面已經看得到的事？** 有就不要加。
3. **替換字放在卡片最下面**（`.subs` 是 `position:absolute` 貼著卡片底部，
   `padBottom()` 會把它的高度讓出來）。**正中央永遠留給英文句子**，
   替換字不可以跑回句子旁邊搶焦點。
4. **中文句子照英文的意思斷字**：`他 是 誰？`／`他 是 我的 哥哥。`
   一個英文字對一段中文，學生一眼就對得起來。對話卡（`pair`）一定要這樣寫。
5. **每一張卡至少一個動畫**：句型卡逐字跳出、語序卡中文飛到英文、變身卡滑過去交換、
   秒懂重點卡一行一行跳出來、對話卡一問一答依序出現、情境畫面一段一段出現。

### 呈現方式兩顆按鈕（使用者指定）

- `🎬 逐字`（預設）：一次只出現一個英文字＋它的中文＋圖示，點卡片出下一個，出完才亮整句中文。
- `📄 整句`：直接整句出來。
- `▶ 自動播`：自動逐字播完再唸整句（語速 0.8 以下時字間隔會自動拉長）。

### Unit 2 的顏色（使用者指定，這是全課的重點）

- `he` **藍底**（`--he`）、`she` **粉底**（`--she`）、`is`／`isn't` **黃底**（`--be`）。
- `swap` 變身卡按 `🔄` 會做 **FLIP 動畫**：藍色和黃色的字**真的滑過去交換位置**，
  句號換問號。學生用顏色就看得出「主詞和 be 動詞換位置」。
  **不要改成淡入淡出**——看得到「移動」才是這張卡存在的理由。
- **句號要往上飄、要放大**（2026-09-21 使用者指定）：變回直述句的時候，
  最後那個 `.` 會**飄上去 ＋ 放大 3.4 倍 ＋ 金色發光**（`.tk.dotup`），
  讓學生永生不忘「**一定要加上句點**」。變成問句時的 `?` 維持原本的 `.d2q`。

## 永遠不溢出（iPad 與任何瀏覽器 100% 流暢的做法）

- 版面 `position:fixed` ＋ `env(safe-area-inset-*)`，直橫轉都不會被瀏海和圓角切到。
- `touch-action:manipulation` ＋ `user-select:none`：連點兩下不會放大、不會選到字。
- 動畫只用 `transform`／`opacity`，不觸發 reflow。
- **不用 `:has()`、`.at()`、`??=`、`structuredClone`**，iOS 14 以上都跑得動。
- **底部按鈕列有幾排，就讓出幾排**：`_shared.js` 的 `fixBar()` 量 `#bar` 的實際高度、
  寫進 `--barH`，五頁的 `#stage` 底部留白都吃這個變數。
  **不要再寫死一個數字**——按鈕一多就會換行（語速六段就讓它變成兩排），
  寫死的結果是替換字被按鈕列蓋掉（2026-09-21 發生過）。
  字體載完、轉向、視窗改變都會重量一次。
- 字卡有 `#cardIn` 內層，`fit()` 量到太寬或太高就**整塊等比縮小**——
  這是「換了長單字（grandmother）也不會爆版」的關鍵，**不要拿掉**。
- iOS 不給自動播語音：第一次 `touchstart`／`mousedown` 會先喚醒語音引擎（`wakeTTS`）。

## 暖身題（22 題）

| 規則 | 實作 |
|---|---|
| 老師決定要不要先做 | 開場閘門：「▶ 開始暖身題」／「⏭ 先不做，直接上句型」 |
| 每題倒數 50 秒 | 大圓環 ＋ 大數字；20 秒轉琥珀、10 秒轉紅並跳動＋嗶聲 |
| 前 20 秒小組討論 | 選項鎖住（「👥 小組討論中」），時間到自動解鎖；老師可按「✋ 提前作答」 |
| 老師可暫停 | `⏸ 暫停` ↔ `▶ 繼續`，圓環與秒數轉灰 |
| 答錯用秒懂方式說明 | 每題 `why` 欄位，一句話講完 |
| 答錯收進錯題庫 | 22 題跑完自動進「🔁 錯題再戰」，**選項順序重洗**，不能用位置背答案 |
| 6 題挑戰題 分數 ✕ 2 | `x2:true`；答對顯示「100 ✕ 2 ＝ +200」。**用「分數 ✕ 2」這個講法，不要寫「雙倍分數」** |
| 四種題型 | 🎧 聽英選英、🎧 聽英選中、👀 看英選中、👀 看中選英（＋💡 觀念題） |

出題判準（不要放寬）：只考兩課教過的東西；打中學生真正會卡住的地方
（who 的用法、`Who's ＝ Who is`、答句開頭要對應、中英語序相反、主詞與 be 換位、
句尾 `is` 不可以縮寫）；錯的選項要有誘答性；**嚴禁瑣碎、冷僻、刁鑽、湊題數**。

## 複習遊戲（10 種，每種 ≧ 20 題；2026-09-21 改成每題 15 秒）

| # | 遊戲 | 玩法 | 題數 |
|---|---|---|---|
| 1 | ⚡ 閃電四選一 | 四選一，連對加分 | 24 |
| 2 | 🔵🩷 他還是她 | 左 He（藍）／右 She（粉） | 24 |
| 3 | 🧩 語序大挑戰 | 照順序點字，排出整句 | 20 |
| 4 | 🔄 變身術 | 直述句 ⇄ 問句 | 20 |
| 5 | 🎧 聽力狙擊 | 聽一句，射下正確的卡 | 20 |
| 6 | 🃏 記憶配對 | 英文配中文，四對一局 | 20 |
| 7 | 🔍 火眼金睛 | 挑出錯字（也有全對的句子） | 20 |
| 8 | ✏️ 填空高手 | 選出少掉的那一個字 | 20 |
| 9 | 🗂 分類大師 | 問句 ❓／直述句 🙋 | 24 |
| 10 | 👑 魔王挑戰 | 打血條，答錯魔王放技能 | 24 |

**三條規則（使用者 2026-09-21 指定，不要改回去）**

1. **每一題限時 15 秒**（不是一整場 2 分鐘）。一場 12 題，記憶配對一局四對給 60 秒。
   時間到 ＝ 答錯，一樣給提示、一樣塞回題庫再出一次。
2. **愈快答對，分數愈高**：`100 ＋ 速度分（剩下秒數 ÷ 15 ✕ 900）＋ 連對 ✕ 20`，再乘倍率。
   **2026-09-21 使用者回報「學生看不懂為何得分」，所以答完是把算式排出來給他看**
   （`judge()` 裡的 `.calc`）：

   ```
   答對 100  ＋  ⚡ 快 剩 12.0 秒 720  ＋  🔥 連對 3 60  ＝  這一題拿到 880
   ［█████████████░░░］ ⚡ 答得愈快，這一條愈長，速度分就愈高
   ```

   有倍率或快答加分就再排第二行 `小計 880 ✕ 2（🎁 驚喜卡）＝ 1760`。
   分數本身也會**跳一下**，旁邊飄一個 `＋880` 上去（`popScore()`）——
   學生看得到分數是「現在」加上去的。答錯就寫明「這一題 0 分」，並說這一題等一下會再出現。
3. **每一個遊戲有自己的 20 張驚喜卡**（`_game_data.js` 的 `SURP`，共 200 張，
   十個遊戲**完全不重複**：閃電是電力、記憶配對是撲克牌、魔王是勇者……）。
   每一場開場先洗牌，**抽過不再抽**。
   **2026-09-21 使用者回報「看不懂驚喜的意義、看不懂文字和圖示、感受不到期待感」，三個修法：**

   | 問題 | 修法 |
   |---|---|
   | 看不懂驚喜是什麼 | 真的畫成**一張卡**：先看到**卡背在抖**，再**翻開**（`#evt .c3`，CSS 3D 翻牌） |
   | 看不懂文字和圖示 | 卡片上**最大的那一行 ＝ 你拿到什麼**（`分數 ✕ 2`／`＋500 分`／`下一題 ＋8 秒`…），下面一行用白話講**現在會發生什麼事**，卡片名字（⚡ 電力全開）退到最小那一行。兩行都由 `eBig()`／`eWhy()` **從效果直接算出來**，不會跟實際發生的事不一樣 |
   | 沒有期待感 | 畫面上**隨時看得到離下一張卡還有幾題**：`🎁 再答對 2 題翻驚喜卡`，剩 1 題會**發光跳動**（`.tg.go.near`）。HUD 有 `🎁 N` 計數，結算會列出**這一場翻到哪幾張**、並提醒這個遊戲一共 20 張 |

   **時間軸（不要改亂）**：答對 ➜ 0.9 秒後卡片出現（先讓學生看完得分算式）➜
   卡片抖一抖 ➜ **翻開的那一刻（＋1 秒）效果才生效、分數才跳上去** ➜ 3.5 秒後出下一題。
   效果不可以在翻開以前就生效，不然學生會「莫名其妙多了幾分」。

   效果有七種：`pts` 直接加分、`x2`／`x3` 倍率、`time` **下一題**加秒數
   （`timeAdd`，在 `run()` 才生效——舊版寫成「這一題」，但驚喜卡是答完才翻的，加了也會被下一題重設掉）、
   `fast` 五秒內答對再加分、`streak` 連對直接加、`shield` 護盾（下一題答錯連對不歸零）。
   **`_game_data.js` 的 `d` 欄位已經不顯示了**（改用 `eWhy()` 自動產生），留著當註解就好。

   **驚喜卡只給好事。** 魔王挑戰答錯時的三個魔王技能改走自己的**紅色橫幅 `#bad`**，
   不可以再借用金色的驚喜卡——學生要一眼分得出「金色 ＝ 獎勵、紅色 ＝ 壞事」。
   三個技能現在都**真的會發生**（下一題只剩 5 秒 `timeAdd=-10`、魔王回血 5%、
   魔王護盾讓下一次答對只扣一半血 `bossShield`），而且橫幅上就寫明下一題會怎樣。
   舊版的「偷走 10 秒」「選項洗牌」其實什麼都沒做，已經改掉，**不要改回去**。

題序與選項每一場都重洗，**同一個遊戲每次重玩都不一樣**——不要拿掉。

**錯題鷹架**：答錯一定給 `h` 提示（一句話講完會卡住的那個點），
而且**那一題會被塞回題庫後面再出一次**，練到會為止；結算畫面列出這一場要記住的每一條。

最佳紀錄存在這台 iPad 的 `localStorage`（`sent_best_<遊戲>`、複習是 `sent_rv_<unit>_<組>`），不會上傳。

## Kahoot 的四個硬限制（不是漏做）

1. **沒有 50 秒**，只能 5／10／20／30／60／90／120，匯入檔用 60 秒。
2. **匯入試算表帶不進「分數 ✕ 2」**，6 題挑戰題要在編輯器手動設。
3. **匯入試算表不能帶音檔**，5 題聽力題改成「老師念，學生選」，要念的句子印在說明文件。
4. **Kahoot 要登入個人帳號，AI 無法代為上傳與試玩。**
   `kahoot_sentences_22_題目與上架說明.md` 最後一節是給老師自己打勾的檢驗清單。

## 驗證（改完一定要做，不可只寫完就回報）

```bash
node sentences/_build.js                 # 重建整站
node sentences/_verify.js                # 量五頁 ✕ 兩尺寸
node sentences/_verify.js unit1.html     # 只量改到的那一頁（省時間）
```

`_verify.js` 在 1024×768 與 820×1180、`offline:true` 下量這些（只印失敗項）：

- **每一頁**：Andika 有載到、站內連結真的存在、沒有 JS 例外、沒有橫向溢出。
- **字卡頁**：每一張不溢出、內容沒有超出卡框、沒有被左右箭頭壓到、進度點對應、
  首末張箭頭狀態、停 6 秒不自動換頁、**逐字模式一開始是藏的且點一下剛好出一個字**、
  **四個 Y/N 開關各自獨立**（關一個不可以把別的也關掉、切回 Y 要回得來）、
  **`🗣 語速` 六段按下去 `RATE` 真的跟著變**（順便確認音效按鈕和 `🐢 放慢` 真的不在了）、
  **一問一答卡的問句和答句都逐字、每一個英文字下面都有中文**、
  **每一張的 `'s` 發音都是「前一個字＋'s」**（/z/，不是 ess）、
  **替換字在英文句子的下方**、**點替換字真的換掉句子裡的字、整句發音也跟著換**、
  **按變身前兩個字真的交換**、撇號真的是紅的、
  **每一張都有情境且關著時不會跑出來**、**情境打開後每一張都看得見且不溢出、縮放不低於 0.62**、
  **`🔤 點中文唸` 切得過去也切得回來**、**每一張的中文都帶著對應的英文**。
  **📝 複習**：組數 ＝ 卡片數 ÷ 4、四選一、倒數 20 秒且真的在減少、答完有回饋、不溢出。
- **暖身題**：閘門→出題→鎖住→提前作答→作答→回饋，每個狀態都不溢出；
  倒數真的在減少；**按暫停真的停住、按繼續真的恢復**；作答後有秒懂說明與下一題；
  停 6 秒不自動跳題。
- **遊戲頁**：大廳剛好 10 種；**十個遊戲逐一打開**，畫面不是空的、有東西可以點、
  **每一題的倒數 15 秒**真的在減少、點一個答案不會壞掉、回得了大廳。

## 絕對不可違反

1. **一張卡只講一件事。** 三年級看不懂就是失敗。
2. **每一句都要有看得見的主詞和畫面。** 不可用抽象名詞（語序、結構、指涉）。
3. **親屬要用台灣學生日常講的詞**（阿姨的小孩，不要寫「媽媽的姊妹的小孩」）。
4. **不自動換頁。** 左右兩條翻頁箭頭（`.nav`，整條邊可按，最小 68px），鍵盤 ← → 也要能用。
5. **字體一律 Andika，字重只有 400／700**（Andika 沒有更粗的，寫 800 會糊掉）。
6. **不考兩課以外的東西。**

## 已知的界線（被問到就翻出來講）

- `dad`／`father`、`mom`／`mother` 用**同一個圖示**——它們本來就是同一個人的兩種叫法，
  圖示不同反而會讓學生以為是兩種人。
  **中文一律寫「爸爸」「媽媽」**（2026-09-21 使用者指定，不要再寫成「爸比」「媽咪」）。
- 使用者原本列的進階替換字裡，`grandmother`、`grandma` 和基礎版重複，已去重，
  進階版留 `aunt`、`cousin`、`niece`。
- 發音走瀏覽器內建的 `speechSynthesis`，**不是預錄音檔**。
  iPad 上是 Apple 的美式語音；不同瀏覽器聽起來會有差，這是瀏覽器決定的，不是網頁決定的。


## 上線（2026-09-20）

已合併進 `main` 並推上 GitHub，網址：

```
https://hsieny627-jpg.github.io/AI-Agent-Open-Code/sentences/
```

根目錄首頁第 8 張卡「💬 英文句型　秒懂教室」連過去。
**製作環境連不到 github.io**（上層網路政策擋掉），所以線上結果由老師自己開一次確認。

## 開新對話怎麼開始最省（照這個做）

**第一句就這樣講：**

> 照 sentences/CLAUDE.md。這次要改的地方：
> 1. …
> 2. …
> （一次把所有修改點列完）

這份規格會自動載入，**不必重述背景、不必叫 AI 先讀檔案、不必再解釋這個網站是什麼**。

### 省額度的五件事（照重要性排）

1. **一次把所有修改點列完**，不要一條一條丟。
   對話愈長，之後每一次回應都要把前面全部重讀一遍——這是最貴的一筆。
2. **指名要改哪一個東西**。講「Unit 2 第 3 張」「暖身題第 5 題」「記憶配對」
   「情境」「替換字」，模型直接改那一個地方；講得模糊它會去翻一堆檔案。
3. **改完只量改到的那幾頁**：`node sentences/_verify.js unit1.html`。
   全量五頁 ✕ 兩尺寸要跑 ~8 分鐘。**只有改到 `_shared.js` 才需要全量**（它影響全部五頁）。
4. **不要在量測跑的時候切分支**（`git checkout` 會把檔案抽走，量測當場白跑）。
   順序永遠是：改完 → build → 量測拿到結果 → 才 commit／merge。
5. **做完一個主題就開新對話。**

### 四個入口，不要碰 `.html`

```bash
node sentences/_build.js         # 一次重建五頁（改 _shared.js 用這個）
node sentences/_build_cards.js   # 只重建 unit1 / unit2
node sentences/_build_quiz.js    # 只重建 warmup
node sentences/_build_games.js   # 只重建 games
node sentences/_build_home.js    # 只重建 sentences/index.html
node sentences/_build_kahoot.js  # 根目錄 kahoot xlsx ＋ 上架說明
```

### 改哪一種東西 → 動哪一個檔（查這張表就好）

| 想改的東西 | 改這個檔 | 然後跑 |
|---|---|---|
| 句子、逐字中文、圖示、底色、替換字 | `_data.js` 的 `U1`／`U2`／`SUB` | `_build_cards.js` |
| **複習題** | `_data.js` 的 `RV1`／`RV2`（每 4 張一組） | `_build_cards.js` |
| **情境**（場景／畫面／什麼時候用） | `_data.js` 最下面的 `SC1`／`SC2` | `_build_cards.js` |
| 暖身題的題目、選項、秒懂說明、挑戰題 | `_quiz_data.js` | `_build_quiz.js` ＋ `_build_kahoot.js` |
| 遊戲題庫、玩法名稱與規則說明、**每個遊戲的 20 種驚喜回饋（`SURP`）** | `_game_data.js` | `_build_games.js` |
| 顏色、字體、按鈕列、發音、語速、音效（目前是空殼） | `_shared.js` | `_build.js`（五頁全部） |
| 字卡的版面與互動（模式、逐字、變身、情境） | `_build_cards.js` | 它自己 |
| 遊戲的玩法邏輯、驚喜事件 | `_build_games.js` | 它自己 |

## 換電腦、換對話、換 AI 怎麼接手（使用者 2026-09-20 問，答案寫死在這裡）

**唯一的真相是 GitHub 上的這個 repo**，不是某一台電腦，也不是某一個對話。
規格、程式、量測腳本全部都在 repo 裡，所以「舊對話睡著了（asleep）」、
「檔案在家裡那台電腦」都不影響任何事——**重開一個新的就好，不要想辦法救舊對話**。

| 情況 | 最簡單、100% 正確的做法 |
|---|---|
| 同一台電腦、開新對話 | 第一句照上面那個模板講。規格會自動載入 |
| **換電腦（學校電腦）** | 開 `claude.ai/code` → 選 `AI-Agent-Open-Code` 這個 repo → **開新的 session** → 第一句照模板講。**不要試著接舊對話** |
| **換 AI（ChatGPT）** | 把 `sentences/CLAUDE.md` 整份貼給它，要它照「檔案分工」那張表改，改完跑 `node sentences/_build.js`。**ChatGPT 跑不了 `_verify.js`（要 Playwright 瀏覽器）**，所以量測要回到 Claude Code 這邊補做，不可以省略 |
| 手機／別台電腦的 Claude 新對話讀不讀得到 `words/CLAUDE.md` | **Claude Code（claude.ai/code）選到這個 repo 才讀得到**；一般的 claude.ai 聊天視窗讀不到（它看不到檔案），要自己把內容貼進去 |

**換誰做都一樣的順序**：改 `_data.js`／`_quiz_data.js`／`_game_data.js` 或產生器
→ `node sentences/_build.js` → `node sentences/_verify.js <改到的那一頁>`
→ 量測 0 失敗才 commit／push。**不可以只改 `.html`**，下一次 build 會蓋掉。

## 待辦（下一個對話可以直接接手）

**只有老師做得到的（AI 做不到，不要假裝做完了）**

1. **在真的 iPad Safari 上點一遍**。製作環境只能在電腦上模擬 iPad 的兩種尺寸，
   **沒有真 iPad**。要看的是：發音清不清楚、`🗣 語速` 六段哪一段最適合三年級、
   `🎞 情境` 和 `🔤 點中文唸` 按起來順不順、左右兩條邊翻頁順不順。
2. **開一次線上網址確認**：`https://hsieny627-jpg.github.io/AI-Agent-Open-Code/sentences/`
   製作環境**連不到 github.io**（上層網路政策擋掉），線上結果 AI 驗證不了。
3. **Kahoot 上架**。要登入個人帳號，AI 上不去。
   匯入檔 `kahoot_sentences_22.xlsx`，步驟與檢驗清單在
   `kahoot_sentences_22_題目與上架說明.md`。

**課堂實測後才知道要不要改的（先不要動，等老師回報）**

4. 情境的文字會不會太長／三年級看不看得懂（2026-09-20 已全部改短，`pic` 還會一段一段跳出來）。
   秒懂重點卡與一問一答卡也一起改過（字放大、一行一件事、一行一行跳出來），
   **看學生第一眼懂不懂，不懂再回報要砍掉哪一行。**
5. 遊戲難度：每題 15 秒會不會太短、驚喜卡跳太頻繁還是太少（現在是每答對 2～4 題一張）。
   **新的得分算式和驚喜卡要看學生講不講得出「我為什麼拿到這些分數」**——
   講得出來就成功了，講不出來再回報是哪一格看不懂。
6. **語速六段哪一段最適合三年級**（預設 0.9）。上課試過再回報要不要改預設。
7. 暖身題 22 題一節課做不做得完（每題 50 秒 ＋ 討論，滿場約 20 分鐘）。

**已知沒做、而且是刻意不做的（不要當成漏掉去補）**

8. 情境用 emoji 不是照片——離線要能上課，而且沒有版權問題。使用者 2026-09-20 同意。
9. 遊戲成績只存在這台 iPad 的 `localStorage`，**沒有排行榜、沒有上傳**。
10. 發音走瀏覽器內建語音，**沒有預錄音檔**。
11. **音效是使用者指定「暫時刪除」的，不是漏掉**（`_shared.js` 的 `SFX` 只留空殼）。
