# 單字小故事網頁 — 規格（接手的 Claude 先讀完再動手）

使用者是國小英語教師。**這份規格是使用者反覆修正後的結果，不要自行「優化」。**

## 這是什麼

一個單字一個獨立網頁，`words/<單字>.html`，單檔、零外部相依。
資料來源是專案根目錄 `family-time-machine-ipad.html` 裡的 `D` 陣列（17 個家人單字）。

## 檔案分工（動手前先看這裡）

| 檔案 | 說明 |
|---|---|
| `_build.js` | **17 個單字頁的唯一產生器。** 樣板和文案都在裡面，`node words/_build.js` 重建全部 17 頁 |
| `<單字>.html` × 17 | **產物，不要手改**，改了會被下一次 build 蓋掉 |
| `_verify.js` | 量測腳本。`node words/_verify.js` 量全部；`node words/_verify.js son.html` 只量一頁 |
| `_build_story.js` | **故事頁的產生器**，`node words/_build_story.js` 重建 why 三頁。17 個單字的場景都在 `FAM` 陣列，兩頁用 `slice` 切開——**改文案只改 FAM 一個地方** |
| `why.html` | 家人單字的故事①（產物）：開場 ＋ 前 9 個字（family…grandfather），10 幕 |
| `why-2.html` | 家人單字的故事②（產物）：開場 ＋ 後 8 個字（grandmother…wife）＋ 結尾，10 幕 |
| `why-more.html` | 附加補充（產物）：六個日常單字（tea／ketchup／hamburger／sandwich／breakfast／goodbye） |
| `brother-why.html` | 四幕實驗檔，等課堂實測結果再決定去留。手工維護 |
| `parts.html` | **單字結構頁**（產物，由 `_build_story.js` 產生）：grand ＝ 大、hus ＝ house、-ther／-ter 的家人字尾，10 幕。字卡的「🧩 結構」按鈕用 `parts.html#幕號` 直接跳進去 |
| `older-younger.html` | **哥哥／姊姊／弟弟／妹妹的說法**（產物，由 `_build_story.js` 產生），7 幕。由 `brother.html`／`sister.html` 的補充按鈕進去 |
| `daughter-gh.html` | **daughter 的重要補充頁**（產物，由 `_build_story.js` 產生）：德文 Tochter／荷蘭文 dochter 的 ch 還在發音，五幕 |
| `_sources.js` | **所有出處來源的唯一真相來源**，四個產生器共用。改出處只改這一個檔 |
| `_phonics.js` | **發音／音標／音節的唯一真相來源**（2026-09-20 新增）。母音紅色、不發音淺灰、IPA／KK 音標、音節切分、點字唸美式發音、放慢語速——全部在這一個檔。**五個**產生器共用 |
| `_build_tree.js` | **family tree 家庭樹的產生器**（2026-09-20 新增），`node words/_build_tree.js` 重建 `family-tree.html`（基礎版）＋ `family-tree-2.html`（進階版） |
| `family-tree.html` | 家庭樹基礎版（產物）：自己家人 7 人，8 幕，**一次只亮一個** |
| `family-tree-2.html` | 家庭樹進階版（產物）：再加上 5 個親戚，7 幕 |
| `_quiz_data.js` | **20 題暖身題的唯一真相來源**（題目／選項／正解／挑戰題／秒懂說明）。網頁版與 Kahoot 都從這裡拿 |
| `_build_quiz.js` | 暖身題的產生器，`node words/_build_quiz.js` 重建 `quiz.html` ＋ `quiz-demo.html` |
| `quiz.html` | 暖身 20 題（產物）：老師按鈕決定要不要先做 |
| `quiz-demo.html` | 只有一題的試玩版（產物），給測網址用，引擎完全相同 |
| `_build_kahoot.js` | Kahoot 匯入檔的產生器，`node words/_build_kahoot.js` 重建根目錄的 `kahoot_family_20.xlsx` ＋ `kahoot_20_題目與上架說明.md` |
| `_build_hub.js` | **首頁的產生器**，`node words/_build_hub.js` 重建<b>專案根目錄</b>的 `index.html` |
| `_words.json` | 17 個單字的清單（由 `_build.js` 產出），總入口讀它，所以不會跟字卡走鐘 |
| `fonts/` | Andika 400 / 700，離線必須 |

**改版面或互動 → 改 `_build.js` 的 `tpl()`，跑一次，17 頁同步。**
**改某一個字的文案 → 改 `_build.js` 的 `WORDS` 陣列，跑一次，只量那一頁。**

## 三幕結構（固定，不要增減）

| 幕 | 內容 |
|---|---|
| ① | 中文意思 ＋ 秒懂圖示（大 emoji）。**副標只在「英文跟中文不一樣」時才放** |
| ② 以前 | 單字最早的樣貌 ＋ 同一個圖示（圖示就是意思，**不加說明文字**） |
| ③ 現在 | 今天的單字 ＋ 一句秒懂收尾 |

**例外：`grandfather` / `grandmother` 的幕② 是「怎麼組的」**（`grand ＋ father`／
`grand ＋ mother`，配一句「**grand ＝ 大　大的 father ＝ 爸爸的爸爸**」）。
（2026-09-20 使用者問「grand 是什麼意思」，原本寫的「長一輩」本來就違反第 5.2 條
的抽象詞禁令，已改成看得見的畫面。）
這兩個字的 `old` 欄位本來就沒有真正的舊拼法，原本的「以前 grand-」是假的快照；
換成構詞規則，學生今天就用得到。使用者 2026-09-19 同意。其餘 15 頁維持「以前」。

## 「單字演變」這件事的結論（2026-09-19 定案，不要再重開）

曾經在單字頁裡做過三個版本，**全部被使用者否決**：

1. 字母逐格變形（brōþor→brothor→brother）→ 違反禁令一
2. 老祖宗分家、拉丁語 pater →「學生看不懂」，且拉丁文學生一輩子用不到
3. 單字搬家、發音不同（🏠→🚶💬→🏡）→「完全看不懂，跟學這個單字沒關係」

定案：

- **17 個單字頁維持三幕，不加演變幕。** 一幕只講一件事。多塞一幕演變，就會擠掉
  學生開口就要用的東西（older/younger、gh 不出聲、parents 的 s）。
- **「字為什麼會變」獨立成故事頁**，不放進單字頁，所以不排擠任何東西。
  使用者 2026-09-19 指定：**主角必須是家人單字，而且 17 個字全部都要有**。
  一個字一幕，每幕配一個秒懂動畫，依 `D` 陣列的教學順序排列。
  19 幕一次放太長（約 8～10 分鐘），**拆成 `why.html` ＋ `why-2.html` 兩頁，各 10 幕**。
  幕上的 `tag` 就是那個字的演變機制：意思變窄／意思變寬／失落的字母／聲音不見了／
  兩群人混合／借來的零件／寶寶先叫出來的。
  日常單字（tea 等六個）是**附加補充**，放在 `why-more.html`，想延伸再放。
  **動畫只能動人、時間、聲音、零件，不可以動整個單字的拼法。**
- **`brother-why.html` 是四幕實驗檔**，等使用者課堂實測。記得住就推廣，記不住就
  刪掉這一個檔——**不要先去動其他 17 頁**。
- **字母逐格變形永遠禁止。** 動畫可以動人、動時間，不要動字母。

## 刪掉廢話（2026-09-19 使用者指定，判準要照著用）

使用者指出 daughter 的「爸爸媽媽的女孩」是廢話，要求**全部字卡刪掉廢話的解釋**。

**判準：這一行有沒有告訴學生「英文跟中文不一樣」的地方？**

- **沒有 → 刪。** 只是把中文再講一次（女兒＝爸爸媽媽的女孩、母親＝媽媽），
  學生看了不會多學到任何東西，只會多佔一行。
- **有 → 留。** 「不分大小」「外公也是」「姑姑、舅媽也是」告訴學生
  **英文一個字管中文好幾個字**。學生下次開口講英文，就會用到這一行。

依這個判準刪掉副標的 7 個字：
`family` `mother` `father` `son` `daughter` `husband` `wife`。
留著副標的 10 個字：
`parent`（爸爸或媽媽）`brother`／`sister`（不分大小）
`grandfather`（外公也是）`grandmother`（外婆也是）
`uncle`（伯伯、舅舅也是）`aunt`（姑姑、舅媽也是）`cousin`（叔叔阿姨的小孩）
`nephew`（外甥也是）`niece`（外甥女也是）。

幕③ 同樣處理：`daughter` 的「念的時候直接跳過它」＝ 把「gh 不出聲」再講一次，刪掉。
`WORDS` 的 `sub` 現在是**選填**，沒有就不會留空行。

**加新字卡時先問這一句：這一行刪掉，學生會少學到什麼？答不出來就刪掉。**

### 2026-09-20 使用者又抓到兩句「學生看不懂」（判準一樣，記下來不要改回去）

| 原本 | 問題 | 改成 |
|---|---|---|
| `father` 幕③「dad 是寶寶先叫的，**不是 father 剪短的**」；`why.html` 同一句 | 「剪短」是**大人的講法**，三年級看不懂；而且整句在講「不是什麼」，學生更難抓 | 字卡：「小寶寶還不會說 father，**先叫出 dad**」；故事頁：「小寶寶還不會說 **father**」＋「先叫出 **da-da**，就變成 **dad**」（**只講看得到的畫面，不講「不是什麼」**） |
| `why.html` sister「**住在一起久了**，唸成今天的 sister」 | **沒有主詞**——使用者直接問「誰住在一起久了？」 | 「**英國人和維京人住進同一個村子**，後來大家都唸 **sister**」 |

**判準再加一條：每一句都要有看得見的主詞。** 主詞說不出是誰，就重寫。

## 單字結構（2026-09-19 使用者指定，**這一節最容易寫出假知識，先讀完**）

使用者問：grand 是什麼意思？mo／fa／bro／ther、sis／ter 分別是什麼意思？
並要求每個單字都加結構動畫、且附 100% 可查證的出處。

**做之前先認清一件事：17 個字裡，只有一部分真的拆得開。**
`mo`、`fa`、`bro`、`sis` **單獨拿出來沒有意思**，查不到任何一手證據。
硬編一個意思出來，學生會記錯，而且違反下面「事實要正確」的禁令。
**查不到就誠實說沒有——`parts.html` 第 7 幕就是專門講這件事的。**

真的拆得開、而且查得到證據的只有這些：

| 結構 | 意思 | 哪些字 | 出處 |
|---|---|---|---|
| `grand` | **大** | grandfather、grandmother | OED「grand, adj.」；旁證 Grand Canyon 大峽谷 |
| `hus` | **house（房子）** | husband | OED「husband, n.」＜ 古北歐文 húsbóndi |
| `-ther`／`-ter` | **家人字的尾巴** | mother、father、brother、daughter、sister | OED 各詞條 ＋ Watkins《AHD of Indo-European Roots》 |
| `ma`→`mo`、`pa`→`fa` | 寶寶最早的音 | mother、father | Jakobson (1960) |
| `＋ s` | 複數（**文法，不是字源**） | parent → parents | Cambridge Dictionary |

**寫 -ther 那一幕時的誠實界線**：教材上只寫「**五個字尾巴長得一模一樣**」這個
**看得到的事實**，不要寫「它們來源全部相同」。因為 `sister` 的 -t- 其實是
s 和 r 中間後來插進去的音，來源和另外四個不一樣（出處卡裡有註記）。
**這個細節不要放上課堂畫面，但也不可以為了整齊就寫成假的。**

剩下 8 個字（`family` `son` `uncle` `aunt` `cousin` `nephew` `niece` `wife`）
**在英文裡拆不開**，`parts.html` 最後一幕就直接告訴學生「拆不開的字整個背起來，
硬拆只會背錯」。**不要為了「每個字都要有」而去編結構。**

字卡上的「🧩 結構」按鈕只給上表那 9 個字，連到 `parts.html#幕號`。
**改 `parts.html` 的幕順序，一定要同步改 `_build.js` 裡 `parts.href` 的幕號。**

## 哥哥／姊姊／弟弟／妹妹（2026-09-19 使用者指定務必補充）

`older-younger.html`（7 幕）。**第 1 幕就是最重要的一句**：

> 平常就說 **my brother**、**my sister**，**不用講大小**。

要分長幼才加形容詞，兩套講法**都是對的**：

| 場合 | 哥哥 | 弟弟 | 出處 |
|---|---|---|---|
| 書面、課本、教學 | **older brother** | **younger brother** | Oxford Learner’s／Cambridge |
| 聊天、口語 | **big brother** | **little brother** | Merriam-Webster |
| 英式、偏正式 | elder brother | — | Cambridge「elder or older?」 |

sister 完全比照。**`elder` 只能放名詞前面**，不可以寫 He is elder than me，
要寫 **older than**——這一點做成第 6 幕，因為學生會在課本上看到 elder。

**2026-09-20 使用者指定改寫**：原本「文字說明太複雜囉嗦」。改寫規則
（下次改這一頁照著用）：

- **一幕最多兩行、一行最多一件事**，看動畫就懂，文字只是補一句。
- 「哥哥 ＝ older brother」改成「哥哥加 **older**」——少掉一個重複的 brother。
- 「elder 只能放在名詞前面」→「**elder 只能放在 brother 前面**」（名詞是抽象詞，
  違反第 5.2 條）。
- 動畫扛主要說明：`.grow`（older 長大）／`.shrink`（younger 縮小）／
  `.drop`（「大的？小的？」自己掉下去不見了，所以平常不用講）／
  `.stamp`（✅ elder brother、❌ He is elder than me 蓋章）。
- 每一個英文字都寫成 `{{單字}}`：母音紅色、不發音淺灰、點下去唸美式發音、可切音標。

**誠實界線**：教材沒有寫「第幾常用」。要做使用次數排名得查 COCA／BNC 語料庫，
製作環境連不上，**沒有做過次數統計**。出處卡裡有這條註記，被問到就翻出來。

## 字卡版面：**只放單字，不放按鈕**（2026-09-20 使用者指定，先讀這一條）

使用者：「音標和音節切換按鈕干擾英文單字學習的版面，請讓單字聚焦，讓版面簡潔，
讓學生一眼就看到英文單字的重點，沒有其他按鈕和不必要文字的干擾。」

所以 **17 張字卡的卡面上只有：圖示 ＋ 單字 ＋ 最多兩行說明**。

- `PH.box(w)` **只回傳單字本身**，不再吐出 chips 與圖例文字。
- 音標三段切換、音節動畫，**全部移到畫面最下方的 `#bar`**
  （`🔤 音標：無／IPA／KK` 循環、`✂️ 音節`，數出來的結果寫在按鈕上，不佔卡面）。
- `▶ 從頭看` 在字卡上**拿掉了**（← 按兩下就回得去，按鈕愈少愈好）。
- 卡面上的紅／灰圖例也拿掉——**顏色本身就是圖例**，再寫一行字是干擾。

### 字卡下方兩排（2026-09-20 新增）

```
#bottom ├─ #wnav   ◀ 上一個單字   n / 17   下一個單字 ▶
        └─ #bar    🔊 念一次 🐢 放慢 🔤 音標 ✂️ 音節 [✨補充] [🧩結構] 🏠 首頁 📖 出處
```

- `#wnav` 直接顯示**前後那個單字本身**（不是「上一頁」），老師一眼知道會跳到哪。
  順序就是 `WORDS` 陣列的順序，**頭尾相接**。
- `#bar` 加按鈕**一定要重跑 `_verify.js`**，按鈕換行會往上吃掉卡片空間。

### 字級（2026-09-20 使用者指定再放大）

| 元素 | 現在 |
|---|---|
| 焦點單字 `.word` | `clamp(50px,min(10.6vh,11.5vw),104px)` |
| 11 個字母以上的長單字（grandfather／grandmother） | `clamp(38px,min(7.5vh,8.2vw),74px)` |
| 中文意思 `.zh` | `clamp(50px,9vh,84px)` |
| 說明 `.sub` | `clamp(19px,2.7vh,26px)` |

**一定要同時夾 `vh` 和 `vw`**（`min(…vh,…vw)`）——只夾 vh 的話，
直式 820×1180 會把 daughter、grandmother 折成兩行。

### 幕① 自動發音（2026-09-20 使用者指定）

翻到「中文意思」那一幕就自動唸一次英文單字（`show()` 裡 `i===0` 也呼叫 `say()`）。
**注意**：有些瀏覽器規定要先有使用者互動才准發聲，所以另外掛了 `armFirstTouch()`
——第一次碰到畫面或按鍵時，如果還停在幕①且還沒唸過，就補唸一次。

## 發音、音標、音節（2026-09-20 使用者指定，**這一節是最大一筆，先讀完**）

全部寫在 `_phonics.js` 一個檔，四個產生器 require 它。**改音標只改 `RAW`。**

| 使用者要的 | 做法 |
|---|---|
| 母音字母標**紅色** | `#FF5A5A`，每一個母音字母都上色 |
| 不發音的字母標**淺灰色** | `#9A9A9A`（niece 的 e、wife 的 e、daughter 的 gh、little 的第二個 t） |
| **音節怎麼切**的秒懂動畫 | 字卡上「✂️ 音節」：先一顆一顆數紅色母音，再把音節拉開，最後寫「N 個母音 ＝ N 個音節」 |
| 幫助記憶與正確發音的動畫 | 按「🔊 念一次」時**一格一格亮過去**，學生看得到哪個字母正在發哪個音 |
| 點任何英文字都能聽 | `PH.autoSay()` 自動把說明文字裡的英文包成可點的 `.sp`；單字本身點 `.phw` |
| **放慢語速** | `#bar` 的「🐢 放慢」，**管全頁所有發音**（rate 0.85 → 0.45） |
| 兩種音標可選 | **全部頁面都用 `#bar` 的「🔤 音標」循環切換**（無 → IPA → KK）。字卡上的 chips 已於 2026-09-20 移除，理由見上一節 |
| **一開始沒有音標** | `mode` 預設 0（無），免得干擾學習。老師按了才出現，選擇記在 `localStorage`，換頁不會跑掉 |
| **每個字母正下方對齊它的音標** | 每一個字母群是一個 `.u`（flex column：字母在上、音標在下），所以一定對齊 |

### 音節切法：**開音節切法**（2026-09-20 使用者指定）

使用者指定 `family` 要切成 **fa．mi．ly**。所以本教材一律用
**開音節切法（母音後面切）**：`fa．mi．ly`、`mo．ther`、`fa．ther`、`bro．ther`、
`daugh．ter`、`un．cle`。

**誠實界線**：辭典（Merriam-Webster）印的 `fam·i·ly` 是**排版斷行用**的連字號，
和「唸的時候怎麼切」不是同一件事。**兩種都對，用途不同**——
出處卡裡把這件事寫清楚了，被問到就翻出來。

### RAW 的寫法

```
單字: '字母|IPA|KK 字母|IPA|KK / 下一個音節…'    不發音寫成  字母|-|-
daughter: 'd|d|d au|ɔː|ɔ gh|-|- / t|t|t er|ɚ|ɚ'
```

產生時**自動檢查兩件事，寫錯直接讓 build 失敗**（不要拿掉這兩個檢查）：

1. 所有字母群接回去要等於這個單字的拼法。
2. **每一個音節剛好一個母音的聲音**——這就是要教給學生的規則。

### 誠實界線（會被學生抓到的地方）

- 課堂上講「一個母音 ＝ 一個音節」，**數的是「畫面上還是紅色、還在出聲」的母音**。
  淺灰色的不出聲、不算；`au`／`ou`／`ie`／`ew` 兩個字母發一個音，畫面上本來就是一格。
  照畫面數，這 32 個字**全部數得對**，所以這條規則對學生是真的，不是近似。
- 音標是**美式**：IPA 用 Cambridge／Oxford 的美式標法，KK 用台灣課本的標法。
  同一個音兩套寫法會不一樣（bed 的 e ＝ IPA `/e/`、KK `/ɛ/`；boat 的 o ＝ IPA `/oʊ/`、KK `/o/`）。
- 聲音是**瀏覽器內建的語音合成**（Web Speech API，`en-US`），不需要連網、不需要帳號，
  但**實際嗓音由老師那台電腦決定**。出處卡裡寫清楚了。
- **舊拼法不要用美式語音唸**（`sweostor`、`brōþor`、`parens`、`nepos`、`wīf`、`tê`…）：
  那些字在 `_build_story.js` 裡包了 `class="nosay"`，`autoSay()` 會跳過。

### 兩個不要踩的地雷

- `PH.autoSay()` **只掃說明文字的容器**（`.sub .mid .note .qtext .big .parts .zh`）。
  `.en` 那種手工排版（`dau<span>gh</span>ter`、`<span>hus</span>band`）拆開來會壞掉，
  那種地方要用 `{{單字}}` 標記（`PH.expand()` 展開），或手工包一個
  `<span class="sp" data-say="整個單字">`。
- **外語單字**寫成 `<span class="sp" data-say="Mutter" data-lang="de-DE">`，
  或 `{{de-DE:Mutter}}`。查不到音標的字由 `PH.plain()` 畫：
  **只標紅母音字母，不硬掰音標、不硬切音節**（誠實界線，不要拿掉）。
  已用的語系：德 `de-DE`、荷 `nl-NL`、西 `es-ES`、法 `fr-FR`。
- 暖身題的**選項是按鈕，故意不做成可點發音**，否則學生想聽發音會變成誤觸作答。
- 暖身題頁**沒有放「🔤 音標」**：那一頁不畫 `.phw` 單字元件，按了不會有反應，
  還會把 `#bar` 擠到第三排。那一頁只放「🐢 放慢」與「🏠 首頁」。
- `#bar` 加按鈕**一定要同步加大 `#stage` 的下方留白**（暖身題頁已改成
  `clamp(118px,15vh,138px)`，放得下換到第二排的按鈕），否則按鈕會蓋住選項。

## family tree 家庭樹（2026-09-20 使用者指定，`_build_tree.js`）

兩頁，**一次只亮一個家人**（一次只給一個學習重點）：

| 檔案 | 內容 |
|---|---|
| `family-tree.html` | 基礎版：me／father／mother／brother／sister／grandfather／grandmother，8 幕 |
| `family-tree-2.html` | 進階版：再加上 uncle／aunt／cousin／nephew／niece，7 幕 |

- 樹上**其他人只留圖示而且變暗**，只有現在這一個會亮起來、跳一下。
- 點樹上任何一個人 → 直接跳到那一幕，而且唸出來。
- **六種顯示切換**（`#modes`，選擇記在 `localStorage` 的 `treeMode`）：

  | 鈕 | 顯示 |
  |---|---|
  | 圖示 | 只有圖示 ＋ 一個大問號（猜猜看用） |
  | 中文 | 只有中文 |
  | 英文 | 只有英文單字 |
  | 英＋圖 | 圖示 ＋ 英文 |
  | 英＋中 | 英文 ＋ 中文 |
  | 全部 | 圖示 ＋ 英文 ＋ 中文 |

- **樹上的節點不顯示中文**，中文只出現在下方的焦點區——
  否則「只呈現英文」那個模式會漏餡。上方原本的 `tag` 也因此拿掉了。
- **這一頁的 `.in` 進場只淡入、不位移**：位移到一半時 `#stage` 的 `scrollHeight`
  會暫時大於 `clientHeight`，`_verify.js` 就會判定溢出。動感交給 `.pop`（只縮放）
  與樹上節點的 `hop`。**不要改回 translateY。**

## 首頁與「回首頁」按鈕（2026-09-20 使用者指定）

- 專案根目錄的 `index.html` 一律叫「**首頁**」，**不要再叫「課堂總入口」**。
  網址：https://hsieny627-jpg.github.io/AI-Agent-Open-Code/
  （寫在首頁畫面上；QR code 是根目錄 `qr-首頁.png`）
- **每一頁都要有一顆「🏠 首頁」**：字卡 17 頁、故事頁 6 頁、家庭樹 2 頁、暖身題 2 頁
  （都在 `#bar`，走 `../index.html`）、`brother-why.html`（手工加的）、
  `about-my-family/index.html`（右上角 `#homeBtn`，不擋投影畫面）。
  **新增任何一頁，記得一起加。**

## 字卡換頁（2026-09-19 使用者指定）

每一幕就是一張字卡，換幕＝**翻卡**（`.turnR` ／ `.turnL`，420ms）。
往後翻從右邊翻進來，往回翻從左邊翻進來；`prefers-reduced-motion` 開啟時不翻。
17 張單字卡另外有一個看得見的卡框 `#card`（圓角、細框、微微的陰影），
`#stage` 只負責 `perspective` 與左右留白。**翻卡動畫必須在 420ms 內結束**——
`_verify.js` 點完「下一頁」等 650ms 才量，動畫太長會量到中間狀態。

**使用者 2026-09-19 確認：字卡裡的「單字演變」就維持翻卡，暫時不改。**
也就是幕②「以前」翻到幕③「現在」兩張快照，中間不畫字母怎麼變。
**不要再提議在 17 張字卡裡加演變幕或字母變形動畫。**

## 出處來源（2026-09-19 使用者指定）

**所有字卡、補充動畫、暖身題都要附 100% 可查證的出處**，但
**附註在最下方、嚴禁佔據版面，以免學習失焦**。做法：

- 出處文字全部寫在 `_sources.js`（`W` ＝ 各單字，`P` ＝ 各頁），**只改這一個檔**。
- **2026-09-20 使用者指定改成「秒懂圖表 ＋ 秒懂動畫」**（原本一條一條的長句太凌亂）。
  每一條出處是一個物件，**三欄分開寫**：

  ```js
  e('這一條在講什麼', '證據出處（工具書 ＋ 條目名稱）', '查到會看到什麼')
  warn('⚠️ 標題', '誠實註記的內容')   // 另外上色，一眼看得出是註記
  ```

  畫出來就是一張三欄表格；最上面一條秒懂動畫依序跑出
  **「📺 課堂上教的 ➜ 📖 出自這一本 ➜ ✅ 你也查得到」**。
  **加新出處一定要照這三欄寫**，不要再塞一整句進去。
- 每頁最下方一個 `<footer id="src">`，平常 `display:none`，**完全不佔版面**，
  所以 `_verify.js` 量到的溢出不受影響。
- `#bar` 多一顆「📖 出處」，按了才蓋上來；Esc 或「關閉」收掉。
  **學生質疑時老師才開**，平常看不到。
- 引用原則：只列**查得到的工具書與條目名稱**（OED、Cambridge、Duden、Van Dale、
  Dictionaries of the Scots Language、Wikipedia 條目、Jakobson 1960），
  不寫「我查過了」這種空話。老師照著條目名稱就查得到。
- **字級（2026-09-20 使用者指定放大）**：表格內文 `clamp(15px,2.05vh,20px)`、
  標題 `clamp(19px,2.6vh,26px)`，**要讓最後一排學生看得清楚**。
  `#src` 平常 `display:none`，放大**不會影響任何一頁的溢出量測**，所以放心改。
- 背景是**全不透明 `#000`**（投影時不可以透出後面的字卡），
  版面 `display:block` ＋ `overflow:auto`，從最上面開始，每一列都捲得到。
  **不可以改回 flex 置中**——內容一超過一個螢幕，上面幾列會被切掉而且捲不回去。
- `_sources.js` 的 `COMMON` 是**音標／音節／發音的共同出處**，
  由 `html()` 自動附在每一頁出處的最後面，不必每個單字重寫一次。

## daughter 的補充頁（2026-09-19 使用者指定）

使用者指定要把「德文 Tochter／荷蘭文 dochter 的 ch 到今天還在發音」做成秒懂動畫。
**沒有放進 `daughter.html`**——17 張單字卡的三幕結構不動（見下面的禁令）——
而是獨立成 `daughter-gh.html`（五幕），由 `daughter.html` 的「✨ 補充」按鈕進去。
`.keep` 是「還在發音的 ch」（一直輕輕震動＋發光），`.mute` 是「英文丟掉的 gh」（暗掉）。
「念一次」支援德文／荷蘭文（場景的 `sayLang` 欄位）。

## 暖身 20 題（2026-09-19 使用者指定）

**介紹全部單字之前先玩**，題目全部出自 17 張單字卡與故事頁的內容。

| 規則 | 實作 |
|---|---|
| 老師決定要不要先做 | 開場是閘門畫面：「▶ 開始暖身題」／「⏭ 先不做，直接上單字」，跳過後隨時可回來 |
| 每題倒數 50 秒，秒數要秒懂 | 上方大圓環 ＋ 大數字；20 秒轉琥珀色，10 秒轉紅色並開始跳動＋嗶聲 |
| **老師可以暫停倒數** | `⏸ 暫停` ↔ `▶ 繼續`，暫停時圓環與秒數轉灰，作答後自動收起。`_verify.js` 會實際量「按了暫停秒數有沒有停住、按了繼續有沒有恢復」 |
| **題目與選項字級要讓最後一排看得清楚** | 題目 `clamp(23px,4vh,38px)`、選項 `clamp(18px,2.8vh,27px)`。**要再放大一定要重跑 `_verify.js`**，1024×768 是最容易擠爆的尺寸 |
| 小組討論後再作答 | 前 **12 秒選項鎖住**（「👥 小組討論中」），時間到自動解鎖；老師可按「✋ 提前作答」提早開放 |
| 答錯用秒懂方式說明原因 | 每題的 `why` 欄位，一句話講完；答對也會用灰字補一次 |
| 6 題答對 **分數 ✕ 2** | `x2:true`。一般題 +100；挑戰題答對顯示 **100 ✕ 2 ＝ +200**。徽章寫「⭐ 挑戰題　答對 分數 ✕ 2」。**用「分數 ✕ 2」這個講法，不要寫「雙倍分數」**（使用者 2026-09-19 指定） |
| 遊戲化 | 分數、連對 🔥、答對／答錯音效（WebAudio，離線可用，可靜音）、答錯畫面會抖一下 |
| 出處 | 同上，`#src` 覆蓋卡 |

**出題判準（不要放寬）**：每題都要對「理解與記住這個單字」有最大幫助；
錯的選項要有**誘答性**；要有**震撼感、跟學生舊認知強烈反差**；
**嚴禁瑣碎、無關、冷僻、刁鑽、湊題數**。
正確答案在 `_quiz_data.js` 裡一律寫成第 0 個，產生時用**固定種子**打散，
所以每次 build 出來的順序一樣，老師對答案不會亂。

### Kahoot 的三個硬限制（不是漏做）

1. **Kahoot 沒有 50 秒**，只能 5／10／20／30／60／90／120，匯入檔用 60 秒。
2. **匯入試算表帶不進雙倍分數**，6 題挑戰題必須在編輯器手動設。
3. **Kahoot 要登入個人帳號，AI 無法代為上傳與試玩。**
   `kahoot_20_題目與上架說明.md` 最後一節是給老師自己打勾的檢驗清單。

## 絕對不可違反

1. **嚴禁交代單字演變過程**。不可做字母逐格變形（brōþor→brothor→brother 這種）。
   學生連現在的單字都背不起來，看拼字怎麼變只會覺得英文更難。
   只給「以前的樣子」和「現在的樣子」兩張快照，中間不畫。
2. **不新增任何字源解釋**。單字頁的所有文字只能從該單字現有的 `story` / `hook` /
   `q` / `a` 欄位濃縮。（`why.html` 不受此限，它不是單字頁；但事實必須正確。）
3. **一頁只講這一個單字**，不可提到別的單字（例如 father 頁不可出現 grandfather）。
   兩個例外，都是使用者指定的，**不要拿掉**：
   - `son` 頁可以出現 `sun`——用同音幫學生記發音。
   - `grandfather` / `grandmother` 的幕② 會出現 `father` / `mother`——那是構詞規則。
4. **不講學生用不到的東西**。拉丁文、語言分支、共同祖先、發音如何改變——全部移除。
   判準：**這一幕能不能幫三年級學生記住「這個字」？** 不能就不要放。
5. **最少的字**。一幕一句話，一句話講一件事。看不懂就是失敗。
   **中文寫法有四條硬規定**（使用者三度抓出問題後定案，不要再犯）：
   1. **不要用比喻去指「這個字以前的樣子」。** 不可寫「老祖宗 X 的意思是」——學生
      會以為在講自己的祖先。要寫「最早寫成 X」「X 以前只有一個意思」。
      「零件」「材料」也一樣，改寫成「從法國借來的 grand」。
   2. **每一行都要落在學生看得見的畫面上。** 不可寫「長一輩」「管事」「任何女人」，
      要寫「爸爸的爸爸」「管這個家的人」「每個女生都能這樣叫」。
   3. **不要用抽象名詞**：意思變寬／變窄、說法、使用、暱稱、規定、指涉。
      標籤也一樣——「意思變窄了」改成「以前算的人更多」。
   4. **親屬要用台灣學生日常講的詞。** 不可寫「媽媽的姊妹的小孩」，要寫「阿姨的小孩」。
   判準：把句子唸給三年級聽，他能不能**立刻指出一個人或一個畫面**？不能就重寫。
6. **不自動換頁**。左右兩邊各一條翻頁箭頭（`.nav`，整條邊都可按，最小 68px 寬），
   老師和學生自己控速。鍵盤 ← → 也要能用。
7. **字體一律 Andika**，而且**字體檔要隨檔案走**（`words/fonts/andika-*.woff2`），
   離線也必須是 Andika，不可只靠 Google Fonts 連線。
   Andika 只有 400 / 700 兩種粗細，**字重不可寫 800 或 900**（瀏覽器會硬加粗，糊掉）。

## 版面

- 全黑底 `#000`；配色沿用：強調 `#9FB4C8`、正文 `#D8D3C5`、白 `#F2F2F2`、
  按鈕底 `#1E1E1E`、框線 `#4A4A4A`
- 尺寸全用 `clamp()` ＋ `vh/vw` 雙限，1024×768 與直式 820×1180 都不得溢出
- `#stage` 左右 padding 必須大於 `.nav` 寬度，否則內容會被箭頭壓到
- `prefers-reduced-motion` 開啟時關掉所有動畫（`.reduce`），但各幕仍要能手動翻

## 驗證（改完一定要做，不可只寫完就回報）

```bash
# JS 語法
perl -0777 -ne 'if(/<script>(.*?)<\/script>/s){print $1}' words/<檔名>.html > /tmp/a.js && node --check /tmp/a.js

# 量測（Playwright 已內建 Chromium）
node words/_verify.js                    # 全部
node words/_verify.js son.html           # 只量改到的那一頁（省額度）
node words/_verify.js --text why.html    # 順便印出每一幕的文字
```

`_verify.js` 會在 1024×768 與 820×1180 兩種尺寸、`offline:true` 下檢查，
**自動分辨兩種頁型**：

- **幕頁**（有 `#dots`：17 張單字卡 ＋ 故事頁 ＋ 補充頁）——
  Andika 有載到（`measureText` 與 sans-serif 不同）、每幕進度點對應、
  無橫向／縱向溢出、內容沒有被左右箭頭壓到、首幕 ← 停用、末幕 → 停用、
  停 6 秒不自動換頁。
- **暖身題頁**（沒有 `#dots`、有 `#go`：`quiz.html` ／ `quiz-demo.html`）——
  閘門／出題／解鎖／作答四個狀態都不可溢出、選項剛好 4 個、
  **一開始必須是鎖住的**（小組討論）、按「提前作答」要真的解鎖、
  **倒數秒數要真的在減少**、作答後選項全鎖、要出現「下一題」與秒懂說明、
  作答後停 6 秒不自動跳題。

- **首頁**（有 `#hub`：專案根目錄 `index.html`，用 `../index.html` 指定）——
  **每一個站內連結都要真的存在**（這條最重要，連結壞掉老師上課才發現最慘）、
  關著的時候不可以有捲軸（投影會被切掉）、按「17 張單字卡」要真的展開且剛好 17 個。

**只印失敗項**——把全部量測資料印出來很貴。
`node words/_verify.js` 不加參數會連 `../index.html` 一起量，不必另外下指令。

```bash
node words/_build.js        # 17 張單字卡
node words/_build_story.js  # why / why-2 / daughter-gh / why-more
node words/_build_quiz.js   # quiz / quiz-demo
node words/_build_kahoot.js # 根目錄的 kahoot xlsx ＋ 說明文件
node words/_build_hub.js    # 根目錄的 index.html（首頁）
```

## 省額度的做法

完整規則見專案根目錄 `AGENTS.md` 第四節（固定規則，永遠適用）。這個資料夾的重點：

- **改版面不要讓模型重打 17 個檔**，改 `_build.js` 跑一次就好（這是最大一筆）。
- **小改動只量那一頁**，不要整批重量。
- **不要每改一次就截圖給使用者看**，全部做完一次回報。
- **每個決定都寫回這份規格**，下次不必重講，也不會被改回去。
- 開新對話時這份規格會自動載入，第一句講「照 words/CLAUDE.md」就夠了。

## 現況（2026-09-20 收工時）

### 產物一覽（26 頁，全部通過 `_verify.js`）

| 類 | 檔案 |
|---|---|
| 字卡 × 17 | `family` `parent` `mother` `father` `brother` `sister` `son` `daughter` `grandfather` `grandmother` `uncle` `aunt` `cousin` `nephew` `niece` `husband` `wife` |
| 暖身題 | `quiz.html`（20 題）、`quiz-demo.html`（1 題試玩） |
| 故事 | `why.html`、`why-2.html`、`why-more.html` |
| 補充 | `parts.html`（單字結構）、`daughter-gh.html`（gh 的聲音）、`older-younger.html`（哥哥弟弟） |
| 家庭樹 | `family-tree.html`（基礎版 8 幕）、`family-tree-2.html`（進階版 7 幕） |
| 實驗 | `brother-why.html`（四幕，等課堂實測） |
| 首頁 | 專案根目錄 `index.html`（由 `_build_hub.js` 產生） |

### 六個產生器（改東西只改這六個，不要碰 .html）

```bash
node words/_build.js        # 17 張字卡
node words/_build_story.js  # why / why-2 / why-more / parts / daughter-gh / older-younger
node words/_build_tree.js   # family-tree / family-tree-2（家庭樹）
node words/_build_quiz.js   # quiz / quiz-demo
node words/_build_hub.js    # 根目錄 index.html（首頁）
node words/_build_kahoot.js # 根目錄 kahoot xlsx ＋ 說明文件
node words/_verify.js       # 量測（改完一定要跑）
```

資料檔：`_sources.js`（出處）、`_phonics.js`（發音／音標／音節）、`_quiz_data.js`（題庫）。

**改了 `_phonics.js` 或 `_sources.js`，四個頁面產生器都要重跑**
（`_build.js`／`_build_story.js`／`_build_tree.js`／`_build_quiz.js` 都把它們內嵌進頁面裡）。

### 已查證並定案的事實（不要再重查、也不要改掉）

- `daughter` 的 `gh` 以前有聲音（中古英語 /x/，約 1500–1700 年消失）。
  旁證：德文 `Tochter`、荷蘭文 `dochter` 的 ch 至今仍發音。
- `family` ＜ 拉丁文 `familia`（含僕役）。
  「Father And Mother, I Love You」是後人倒推的 backronym，**查無一手出處**。
- `grand` ＝「大」（OED），旁證 Grand Canyon。`hus` ＝ house。
- `-ther`／`-ter` 是家人字的尾巴；**但 `sister` 的 -t- 來源不同**，
  所以教材只寫「尾巴長得一樣」這個看得到的事實。
- `mo`／`fa` 來自寶寶的 ma／pa；**`bro`／`sis` 單獨沒有意思**，不要編。
- **`sister` 與維京人（2026-09-20 訂正，重要）**：
  教材原本寫「英國人和維京人**住進同一個村子**」——**查不到任何一個村子的紀錄**，
  那句是編的，已經刪掉。查得到的只有這三條，出處卡裡都列出來了：
  1. `sister` 這個形帶有古北歐文 **systir** 的影響（OED「sister, n.」）。
  2. 維京人在**英格蘭東北部**定居，是**一整片地區**（Anglo-Saxon Chronicle 876–877 年條、
     Danelaw 丹麥區條目），**不是某一個村子**。
  3. 地名就是證據：**-by** ＝ 古北歐文的「村子」——Grimsby、Whitby、Derby、
     Scunthorpe（-thorpe）（OED／Cambridge「-by, suffix」）。
  **不要再把「某一個村子」寫回去。**

### 首頁（2026-09-19 完成，2026-09-20 改名並加上回首頁按鈕）

專案根目錄 `index.html`，由 `words/_build_hub.js` 產生。老師只要記**一個網址**：
https://hsieny627-jpg.github.io/AI-Agent-Open-Code/
（網址寫在首頁畫面上；QR code：根目錄 `qr-首頁.png`，指向同一個網址）
**其他每一頁都有一顆「🏠 首頁」連回這裡。**

照上課順序排 7 步：
`1 🎯 暖身 20 題` → `2 📖 家人單字時光機`（學習＋遊戲）→ `3 🃏 17 張單字卡`（點開才展開）
→ `4 🌳 family tree 家庭樹` → `5 🧩 單字結構` → `6 📜 單字故事`
→ `7 🎬 About My Family 影片`。
最下面一排小字是延伸頁與老師專用（Kahoot 說明）。

**界線，不要跨過去：**

- **這是一頁純連結頁**，只放連結與一句說明，**不放任何教學內容**。
- **絕對不要去改 `family-time-machine-ipad.html`**。那支 84KB 單檔 app 本來就能跑，
  動它風險最高、花的額度也最多。總入口只是連過去。
- 17 張卡的清單讀 `words/_words.json`（`_build.js` 產出）。
  **增刪單字 → 跑 `_build.js` 再跑 `_build_hub.js`**，兩邊自動同步。
- **關著的時候不可以有捲軸**（那是老師投影出來的樣子），展開後才可以往下捲。

### 待辦（下一個對話可以直接接手）

1. 首頁做好了，但**還沒有人實際在教室投影過**。
   第一次上課前，老師用教室那台電腦開一次，確認投影出來字夠大、連結都點得到，
   並且**實際點一個單字聽聽看發音**——語音合成的嗓音由那台電腦決定，
   有些電腦第一次要先去系統設定裡裝英文語音。
2. `brother-why.html` 的課堂實測結果 → 決定推廣或刪除。
   （這個檔是**手工維護**的，只補了「🏠 首頁」，**沒有**音標／音節／點字發音——
   實測後若要留，再決定是否併進 `_build.js`。）
3. **Kahoot 要老師本人登入上傳並試玩**（AI 沒有帳號，做不到這一步）。
4. 出處目前是**列條目名稱**供老師查，製作環境連不上 etymonline／Wikipedia／
   COCA，**沒有逐條開網頁核對過**。老師第一次上課前建議抽查兩三條。
