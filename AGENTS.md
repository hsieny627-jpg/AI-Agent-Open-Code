# AGENTS.md — 本資料夾的工作流記憶（給所有 AI Agent 讀）

> 本檔讓每個新開啟的 Agent 都知道：使用者有一套「YouTube 字幕知識庫工作流」，
> 且使用者希望——**當他說「我不懂」或提到這個工作流時，主動提示他流程、步驟與更有效率的方法**。

---

## 一、使用者最重要的工作流：YouTube 字幕知識庫（第二個大腦）

**出處**：三師爸 Sense Bar 的 `sensebar-agent-knowledge-vault-builder`
（https://github.com/mathruffian-dot/sensebar-agent-knowledge-vault-builder）
**詳細秒懂圖表**：見本資料夾 `工作流_秒懂圖表_YouTube字幕知識庫.md`。

**一句話**：把某個 YouTube 頻道的影片字幕自動抓下來 → 清洗成逐字稿 → 放進三層 Obsidian 知識庫 → Agent 每週自動整理 → 用來備課／做教案／出考題。

### 五步驟（速記）
1. `extract_videos.py` — 掃 @sensebar 頻道，用關鍵字（claude、codex、antigravity、opencode、agent）過濾影片，輸出 `sensebar_ai_urls.txt`。
2. `download_all_subs.py` — 讀網址清單，用 yt-dlp 下載字幕。
3. 腳本內建清洗 — 去 VTT 標頭、時間碼、HTML 標籤、刪重複滾動行，存成乾淨 Markdown。
4. 三層資料夾 — `Clipping/`（原始逐字稿，不改）、`創作庫/`（使用者原創教材，不改）、`知識庫/`（Agent 全權管理）。
5. 每週重整 — 掃新檔 → 摘要 → 歸類 → 健康檢查 → 更新索引與日誌。

### 運行方式（依賴）
- `pip install yt-dlp`（建議用 `.venv`）。
- 換主題／換頻道：改 `extract_videos.py` 開頭的 `keywords` 或頻道名稱。

---

## 二、使用者習慣與規則（必須遵守）

1. 使用者是**老師，不懂程式**。解釋要簡單、圖表化，多用比喻。
2. 本資料夾的 `AGENT_SETUP_*.md` 檔是「寫給 Agent 讀的設定指南」；使用者說「讀這份檔案」時要照做。
3. 本資料夾已有三層知識庫結構與兩個 Python 腳本（工作流已複製完畢），且已實測跑通一遍：@sensebar 452 支影片過濾出 89 支 AI 相關，已下載清洗 **89 份逐字稿到 `Clipping/`**（62 份含字幕內容、27 份無字幕），執行環境為本資料夾 `.venv`（已裝 yt-dlp）。
4. 知識庫已完成**第一次重整**（2026-08-02）：`知識庫/` 有 7 大主題資料夾各含 `主題總覽.md`、以及總 `索引.md` 與 `日誌.md`。使用者說「重整知識庫」時，依 `索引.md` 底部「每週重整 SOP」執行。
5. 半自動原則：先列計畫給使用者看，OK 再動手；**一次只做一件事**。
6. 使用者偏好「秒懂圖表」風格（mermaid 流程圖＋速覽表＋為什麼表格）。
7. ✅ 已完成（2026-08-11）：新增 `知識庫/07_教學應用/大人學破局思考_聽書秒懂對照表.md`，33 章全數找到對應／近似集數（24 章 ✅ 直接可聽、9 章 🔶 近似）。Obsidian vault 路徑：`G:\我的雲端硬碟\AI  Agent  - Ob\AI  Agent  1`（對照表已同步複製一份進去）。
8. ✅ 已完成（2026-08-13）：完成游皓雲老師部落格（yuhaoyun.world）知識庫報告製作：
   - `游皓雲老師_班級經營教學分析報告.md`（本資料夾）＋ 桌面 Word 版（含網址版與無網址版）。
   - 複製一份到 Obsidian：`G:\我的雲端硬碟\AI  Agent  - Ob\AI  Agent  1\游皓雲老師_教學知識庫\`。
   - 嚴謹審查發現：原報告只涵蓋「教學技巧」分類 63 篇，**遺漏「語言教學／語言學習」相關文章 107 篇**（學習方法 51、西班牙語 29、專業精進 14、外國人學中文 4、英語/法語/日語/俄語/數位工具 9）。
   - 審查結論存於 `審查報告_語言教學語言學習涵蓋率.md`。
   - ⚠️ 品質教訓（2026-08-13）：使用者抓出報告第 4 篇「學生都不回答」只寫了「有四技巧」卻未列出內容；已補齊四個技巧並同步三處（md／Obsidian／Word 兩版）。**教訓：萃取重點時「標題承諾的內容」必須全部列出來，不可只寫一句話帶過。**
9. 🚧 進行中（2026-08-13）：擴充「語言教學＋語言學習」知識庫報告（目標 107 篇遺漏文章全部萃取重點）：
   - ✅ 已完成：小語種＋數位工具 9 篇（英語2/法語1/日語2/俄語2/數位工具2）重點萃取（結果僅存於對話，未存檔）。
   - ❌ 未完成（子代理回傳空，需重跑）：學習方法 51 篇、西班牙語 29 篇、專業精進14＋外國人學中文4＝18 篇。
   - 📋 萃取清單：`C:\Users\ＪＤ\AppData\Local\Temp\opencode\relevant_113.txt`（107 篇 slug/標題/日期/分類）。
   - ➡️ 下一步：重跑 3 個失敗批次 → 合併全部重點 → 撰寫完整知識庫報告 → 交叉驗證 107 篇無遺漏。

10. ✅ 已完成（2026-09-20）：新增**英文句型網站** `sentences/`（三年級 Unit 1／Unit 2）。
   四個部分：首頁／暖身 22 題／Unit 1 ＋ Unit 2 句型卡／10 種複習遊戲（每種 ≧ 20 題、每場 2 分鐘）。
   規格寫在 `sentences/CLAUDE.md`，**接手前先讀那一份**。
   改內容只改 `_data.js`／`_quiz_data.js`／`_game_data.js`，然後 `node sentences/_build.js`，
   再跑 `node sentences/_verify.js` 量測（五頁 ✕ 兩尺寸，只印失敗項）。
   根目錄首頁第 8 張卡連過去；Kahoot 匯入檔是 `kahoot_sentences_22.xlsx`。
   **2026-09-20 第二次改版**（使用者指定三大重點：版面簡潔清爽、廢話少、秒懂動畫多）：
   字體放大、廢話全刪、四個 Y/N 開關（英文／中文／圖示／整句中文）、替換字移到卡片最下面、
   `'s` 唸 /z/、替換字會連整句發音一起換、五頁都有 `🔊 音效 Y/N`、
   情境與秒懂重點都改短並加上動畫。細節全部寫在 `sentences/CLAUDE.md`。
   **2026-09-21 第三次改版**（使用者 19 點指定）：
   Unit 1 加到 16 張、Unit 2 加到 21 張（新增 8 張「縮寫變身」秒懂動畫卡、2 張職業填空卡）；
   `Who` 的 w 淺灰（不發音）、`is` 的 i ／ `not` 的 o 紅色、Unit 1 的 she 改淺粉底；
   一問一答加替換字＋藍底 he ➜ He 飛行動畫；`is` 的圖示改成「＝」（🟰 在某些電腦是白框）；
   **發音引擎整個換成排隊式**（修掉自動播漏字、整句延遲、點了沒聲音），`＝` 不發音；
   兩個 Unit 各加 `📝 複習`（每 4 張一組、20 秒限時、愈快分數愈高）；
   10 個遊戲改成**每題 15 秒、速度分、每個遊戲 20 種專屬驚喜回饋**（共 200 種不重複）。
   **2026-09-21 第四次改版**（使用者 10 點指定）：
   **音效與音效按鈕暫時刪除**（`_shared.js` 的 `SFX` 只留空殼，要加回來只改那一段）；
   dad ＝ 爸爸、mom ＝ 媽媽；`What` 的 h 淺灰（不發音）；
   **縮寫變身卡改五拍**（完整聽到 `Who is` 和 `Who’s`，最後亮出 `Who is ＝ Who’s`）；
   **縮寫等式卡會自己演一次**（兩句各自亮起來並完整唸出來，最後 ＝ 放大）；
   語序卡的 `Who` 不再折行（`.chip` 是直向 flex，英文要包在 `.cen` 裡）；
   **一問一答改成逐字**（每個英文字正下方就是它的中文）；變身術的句點往上飄＋放大；
   **遊戲改成看得懂**（得分排成算式＋速度條、驚喜改成會翻開的卡、翻開才生效、
   隨時顯示「再答對 N 題翻驚喜卡」、魔王技能改走紅色橫幅且三個技能都真的會發生）；
   **語速六段 0.5～1.0**（1.0 ＝ 正常，預設 0.9）取代 `🐢 放慢`。
   量測 402 項 0 失敗，已合併進 `main`。
   **2026-09-24 第五次改版**（使用者 14 點指定，句型網站＋單字網站一起改）：
   整句發音照畫面唸縮寫（Who’s he?／He’s my father.）；**念到哪一個字那個字就放大變亮**；
   一問一答字放大；問號動畫放大 4.6 倍停 1.8 秒；`📑 目次`；情境改成兩個人演的小劇場；
   暖身題／複習／遊戲**答錯蓋一整頁、倒數 3 秒、結束有答錯整理**；
   單字網站：時光機加回首頁、family tree 逐字中文、**出處改成一條一頁**、
   字的結構頁（德文／荷蘭文先猜再公布、sister 的 t 考證、-ter 動畫）、
   新頁 `words/world.html`（地圖演語言演變、五國家人單字猜猜看）、根目錄 `offline.html`（離線上課）。
   細節在 `sentences/CLAUDE.md` 與 `words/CLAUDE.md` 最上面的「2026-09-24」那一節。
   **換電腦／換對話／換 AI 怎麼接手，那份檔案裡有一張表，照那張表做。**

---

## 三、提示機制（重要）

**觸發時機**：使用者說「我不懂／我忘了／這個工作流是…？」或提到知識庫、字幕、逐字稿、三層結構、每週重整等關鍵字時，**必須主動提示**：

1. **流程**：五步驟（抓清單 → 下字幕 → 清洗 → 三層庫 → 每週重整）。
2. **步驟**：指出現在卡在哪一步，下一步做什麼。
3. **怎麼做更有成效**：
   - 固定每週一個時段重整（例如週日），不積壓。
   - 關鍵字就是濾網，主題變了改 `keywords` 一行即可。
   - 換頻道也只要改一行。
   - 善用第 5 步健康檢查與 Obsidian Graph View。
   - 別全域 pip install，用 `.venv`。

回答時請簡短、圖表化（可引用 `工作流_秒懂圖表_YouTube字幕知識庫.md`）。

---

## 四、固定規則：用最省的額度，做最高品質的教學網站（永遠適用）

使用者 2026-09-19 指定：**以後所有教學網頁的工作一律照這套規則，不必再問。**

### 品質（不可打折）

1. 先讀該資料夾的規格檔（例如 `words/CLAUDE.md`），照做，**不自行「優化」**。
2. 產出必須**量測**，不可目視、不可只寫完就回報。量測腳本要留在 repo 裡重複使用
   （例：`words/_verify.js`）。
3. 判準永遠是「**這能不能幫學生記住這個東西**」。不能，就砍掉，不管它多有趣。
4. 一幕一句話，一句話講一件事。學生看不懂就是失敗。
5. 事實要正確。不確定的字源或說法，寧可不放進教材。
6. 不同意使用者的作法時，先講理由並提替代方案，**等使用者同意才動手**。

### 額度（照這個順序省）

1. **重複的東西寫成腳本，不要讓模型重打。** 多頁共用樣板就做產生器
   （例：`words/_build.js`，改版面只改一個地方跑一次，輸出量降到 1/N）。
2. **只量改到的那一頁**，整批重量只在改樣板時做。
3. **量測只印失敗項與一行總結**，不要把全部量測資料印出來。
4. **不要一頁一頁截圖給使用者看**，全部做完一次回報。
5. **大檔先用 grep/sed 取需要的幾行**，不要整份讀進來。
6. 使用者一次講完所有修改點；模型一次做完再回報。
7. **每個決定都寫回規格檔**，下次不必重講，也不會被下一個對話改回去。
8. 做完一個主題就開新對話——對話越長，之後每一次回應都要重讀前面全部內容。
